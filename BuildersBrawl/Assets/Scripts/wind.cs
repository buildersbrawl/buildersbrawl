using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class wind : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //bool blowing is false
        //int pinwheelSpeed is 1
    }

    // Update is called once per frame
    void Update()
    {
        //set timer random duration 15-30s
        //when timer goes off, blowing is true
        //set another timer
        //while blowing is true, players move to the left
        //pinwheelSpeed is 5
        //when timer goes off, blowing is false
        //pinwheelSpeed is 1
        //set another timer


    }
}
